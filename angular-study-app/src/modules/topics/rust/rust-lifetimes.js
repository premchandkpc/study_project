(function() {
  var topic = {
    id: "rust-lifetimes",
    area: "rust",
    title: "Rust Lifetimes",
    tag: "Memory",
    tags: ["rust","lifetimes","references","dangling"],
    concept: "// TODO — coming soon",
    visual: function(mount) {
      mount.innerHTML = '<div style="padding:20px;color:#768390;font-size:13px">⚙️ Rust Lifetimes — interactive visual coming soon.</div>';
    },
    gotchas: [],
    interview: [],
    tradeoffs: "// TODO"
  };
  window.RUST_TOPICS = (window.RUST_TOPICS || []).concat([topic]);
})();
