(function() {
  var topic = {
    id: "db-postgres-internals",
    area: "databases",
    title: "PostgreSQL Internals",
    tag: "PostgreSQL",
    tags: ["postgres","toast","vacuum","autovacuum","pg-stat"],
    concept: "// TODO — coming soon",
    visual: function(mount) {
      mount.innerHTML = '<div style="padding:20px;color:#768390;font-size:13px">⚙️ PostgreSQL Internals — interactive visual coming soon.</div>';
    },
    gotchas: [],
    interview: [],
    tradeoffs: "// TODO"
  };
  window.DB_TOPICS = (window.DB_TOPICS || []).concat([topic]);
})();
