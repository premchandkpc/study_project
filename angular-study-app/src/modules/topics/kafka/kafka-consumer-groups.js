(function() {
  var topic = {
    id: "kafka-consumer-groups",
    area: "kafka",
    title: "Kafka Consumer Groups",
    tag: "Messaging",
    tags: ["kafka","consumer-groups","rebalance","offsets"],
    concept: "// TODO — coming soon",
    visual: function(mount) {
      mount.innerHTML = '<div style="padding:20px;color:#768390;font-size:13px">⚙️ Kafka Consumer Groups — interactive visual coming soon.</div>';
    },
    gotchas: [],
    interview: [],
    tradeoffs: "// TODO"
  };
  window.KAFKA_TOPICS = (window.KAFKA_TOPICS || []).concat([topic]);
})();
