(function() {
  var topic = {
    id: "kafka-producer-consumer",
    area: "kafka",
    title: "Kafka Producer & Consumer",
    tag: "Messaging",
    tags: ["kafka","producer","consumer","batching","acks"],
    concept: "// TODO — coming soon",
    visual: function(mount) {
      mount.innerHTML = '<div style="padding:20px;color:#768390;font-size:13px">⚙️ Kafka Producer & Consumer — interactive visual coming soon.</div>';
    },
    gotchas: [],
    interview: [],
    tradeoffs: "// TODO"
  };
  window.KAFKA_TOPICS = (window.KAFKA_TOPICS || []).concat([topic]);
})();
